<?xml version="1.0" encoding="UTF-8"?>
<project name="jikesPGCommon" basedir=".">
    <property name="basews" value="${ws}"/>
    <property name="baseos" value="${os}"/>
    <property name="basearch" value="${arch}"/>
    <property name="basenl" value="${nl}"/>

    <!-- Compiler settings. -->
    <property name="javacFailOnError" value="true"/>
    <property name="javacDebugInfo" value="on"/>
    <property name="javacVerbose" value="false"/>
    <property name="javacSource" value="1.5"/>
    <property name="javacTarget" value="1.5"/>
    <property name="compilerArg" value=""/>

    <echo message="java.home = ${java.home}"/>
    <echo message="ant.home = ${ant.home}"/>
    <echo message="eclipse.home = ${eclipse.home}"/>

    <!-- Following assumes we're invoking the build from within some plugin project's top-level directory -->
    <xmlproperty file="../org.eclipse.safari.jikespg.feature/feature.xml" prefix="jikespg"/>

    <echo message="Building for SAFARI JikesPG IDE feature version ${jikespg.feature(version)}"/>

    <property name="jikespg.version" value="${jikespg.feature(version)}"/>

    <path id="path_bootclasspath">
        <fileset dir="${java.home}/lib">
            <include name="*.jar"/>
        </fileset>
	<fileset dir="${ant.home}/lib">
            <include name="*.jar"/>
        </fileset>
    </path>

    <property name="bootclasspath" refid="path_bootclasspath"/>

    <condition property="eclipse.home" value="/usr/lib/eclipse-3.1">
        <and>
            <not>
	        <isset property="eclipse.home"/>
	    </not>
            <os family="unix"/>
	</and>
    </condition>

    <condition property="eclipse.home" value="f:/eclipse-3.1.2">
        <and>
            <not>
	        <isset property="eclipse.home"/>
	    </not>
            <os family="windows"/>
	</and>
    </condition>

    <path id="path_eclipse.build.path">
        <fileset dir="${eclipse.home}/plugins">
            <include name="**/*.jar"/>
        </fileset>
    </path>
    <pathconvert property="eclipse.build.path" refid="path_eclipse.build.path" targetos="unix"/>
</project>
